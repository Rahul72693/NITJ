#include<stdio.h>
int main()
{
	int a;
	printf("enter an year");
	scanf("%d",&a);
	if(a%4==0)
	{
		if(a%100==0)
		{
			if(a%400==0)
				printf("it is a leap year");
			else
				printf("it is not a leap year");
		}
		else
			printf("it is a leap year");
	}
	else
		printf("it is not a leap year");
}
