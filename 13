#include<stdio.h>
int main()
{
	char a;
	printf("enter a string");
	scanf("%c",&a);
	if(a>=65&&a<=90)
	{printf("it is an alphabet");}
	if(a>=97&&a<=122)
	{printf("it is an alphabet");}
	if(a>=48&&a<=57)
	{printf("it is a single digit number");}
	
}
